describe("RateLimitService", () => {
  describe("canActivate", () => {
    it("should return true if ConnectionList's allowed method returns true");
    it("should return false if ConnectionList's allowed method returns false");
  });
});
